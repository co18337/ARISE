/// The four core stats from the game model. Every completed task grants XP
/// to exactly one of these.
enum StatType {
  str, // strength (gym)
  sta, // stamina (running/cardio)
  dis, // discipline (sleep & wake adherence)
  rec, // recovery (diet, skincare, hydration)
}
